apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "credhub.fullname" . }}-test-connection"
  labels:
{{ include "credhub.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "credhub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
