#!/usr/bin/env bash

set -ex

cp "${BOSH_COMPILE_TARGET}/credhub/credhub.jar" "${BOSH_INSTALL_TARGET}"

mkdir -p "${BOSH_INSTALL_TARGET}/keystore-helper"
cp "${BOSH_COMPILE_TARGET}/keystore-helper/PemToKeyStore.class" \
  "${BOSH_INSTALL_TARGET}/keystore-helper/"
